%p#notice= notice
= render 'layouts/categories'
= render 'products/product_list', header: @category.name
= link_to 'Edit', edit_category_path(@category) if current_user.try(:admin?)
= link_to 'Back', :back
