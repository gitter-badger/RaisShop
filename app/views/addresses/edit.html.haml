%h1 Editing address
= simple_form_for(@address) do |builder|
  = render 'addresses/address_fields', f: builder
  = builder.button :submit
= link_to 'Show', @address
|
\#{link_to 'Back', addresses_path}
